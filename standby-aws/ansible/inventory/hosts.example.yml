all:
  children:
    aws_standby:
      hosts:
        aws-standby-01:
          ansible_host: REPLACE_WITH_STANDBY_EIP
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/eth-failover-hoodi.pem
